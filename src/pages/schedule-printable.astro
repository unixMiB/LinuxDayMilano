---
import { getCollection } from "astro:content";
import SchedulePrintableJS from "../layouts/schedule-printable";

import Layout, { HeroEnum } from "../layouts/layout.astro";

const allSchedulesYaml = await getCollection("schedules");
---

<Layout id='index' title='Programma' hero={HeroEnum.small}>
  <!-- <SchedulePrintableJS client:load allSchedules={allSchedulesYaml} /> -->
</Layout>
